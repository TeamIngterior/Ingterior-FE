import styled from 'styled-components';
import { theme } from '@assets/styles/theme';

export const AddConstructionContainer = styled.div``;

export const AddConstructionForm = styled.form`
  margin-top: ${theme.gap.gap7};
`;
