import { DefaultTheme } from 'styled-components';

const layoutComponent = {
  header_height: 100,
  footer_height: 100,
};

const color = {
  primary: '#3182f6',
};

const theme: DefaultTheme = {
  layoutComponent,
  color,
};

export { theme };
